services:
  - type: web
    name: youtube-telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r bot-requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
